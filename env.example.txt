# ============================================
# Firebase Configuration
# ============================================
# Get these from Firebase Console → Project Settings → General → Your apps
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================
# EmailJS Configuration
# ============================================
# Get these from EmailJS Dashboard → Email Services → Integration
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_VERIFICATION=your_template_id
VITE_EMAILJS_TEMPLATE_BOOKING=your_booking_template_id
VITE_EMAILJS_TEMPLATE_PASSWORD_RESET=your_password_reset_template_id

# ============================================
# Cloudinary Configuration
# ============================================
# Get these from Cloudinary Dashboard → Settings → Upload
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# ============================================
# PayPal Configuration
# ============================================
# For testing: VITE_PAYPAL_ENV=sandbox
# For production: VITE_PAYPAL_ENV=production
VITE_PAYPAL_CLIENT_ID=your_paypal_client_id
VITE_PAYPAL_ENV=sandbox

# ============================================
# App Configuration
# ============================================
# The public URL where your app is deployed
# Development: http://localhost:8080
# Production: https://your-project-id.web.app
VITE_APP_URL=http://localhost:8080

# ============================================
# Optional: Emulator Mode
# ============================================
# Set to 'true' to use Firebase Emulators (development only)
VITE_USE_EMULATORS=false

# ============================================
# PRODUCTION DEPLOYMENT NOTES:
# ============================================
# For production deployment, create a .env.production file with:
# - All production Firebase credentials
# - VITE_PAYPAL_ENV=production (if ready for live payments)
# - VITE_APP_URL=https://your-project-id.web.app
# - VITE_USE_EMULATORS=false
# 
# Then build with: npm run build:prod
# And deploy with: npm run deploy

